name = "sveltia-cms-auth"
main = "src/index.js"
compatibility_date = "2023-03-24"

# Runtime vars are set explicitly by the deploy command in package.json
# (`wrangler deploy --var ...`), with the secret sourced from the Workers
# Builds build variable $GITHUB_CLIENT_SECRET. This avoids the keep_vars /
# dashboard-wipe pitfalls — every deploy sets all three bindings directly.
